# HKBA Phase 2 M0–M1 实施计划：基线准备与数据层新表

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Spec 来源（全部已获用户批准）：**

- [HKBA 轻量页面构建后台总设计](../specs/2026-07-15-hkba-lightweight-page-builder-design.md)
- [组件目录与配置规范](../specs/2026-07-15-hkba-component-catalog.md)
- [数据、API 与发布规范](../specs/2026-07-15-hkba-data-api-publishing.md)
- [UI 与交互规范](../specs/2026-07-15-hkba-ui-interaction-system.md)
- [验收、迁移与运维规范](../specs/2026-07-15-hkba-acceptance-migration-operations.md)

**已批准决策（D1–D10，2026-07-18 全部批准）：**

- **D1 架构延续**：保留 Next.js 16 + React 19 + Express 4 + SQLite（better-sqlite3）与宝塔单域名代理部署，渐进升级，不重写框架、数据库或部署链路。（主设计 §1、§13；验收 §15）
- **D2 模型分层**：页面身份与页面版本分离（Page / PageVersion / PageBlock），草稿与正式版本并存，正式站点只读已发布版本。（数据规范 §2.1–2.3）
- **D3 ID 策略**：新表统一使用文本 UUID 主键；现有旧表保留整数 ID；通过映射表桥接新旧 ID。（数据规范 §2.1；组件目录 §1 通用字段）
- **D4 迁移纪律**：数据库变更采用版本化 migration 机制；破坏性 schema 变更遵循"扩展—迁移—收缩"，不在一次部署中直接删除旧字段。（验收 §10、§15）
- **D5 旧字段保留**：`pages.content_*` 与 `news.content_*` 保留至旧数据迁移验证与回滚窗口结束。（验收 §15）
- **D6 组件注册表**：组件定义以代码注册表为准，数据库只保存可搜索的同步元数据，不允许后台创建任意组件代码。（数据规范 §2.7）
- **D7 新闻单一实体**：新闻只维护一份；页面组件保存查询条件不复制正文；年份规则 `displayYear ?? year(publishedAt)`。（数据规范 §1、§6）
- **D8 媒体引用**：组件以稳定 ID 引用 `MediaAsset`，`MediaReference` 记录引用关系，不把临时上传 URL 当作内容数据。（数据规范 §2.6）
- **D9 双语同版本**：中英文组件属于同一页面版本、一次发布同时切换；缺失翻译作为发布检查项，不创建互相漂移的双版本。（数据规范 §2.2）
- **D10 存储路线**：首期继续使用 SQLite + 本地上传；PostgreSQL 与对象存储迁移在接口边界稳定后单独规划，不与页面构建器首期上线绑定。（主设计 §13；验收 §15）

**Goal:** M0 建立可安全演进的工程基线（实施计划、只读数据盘点、版本化 migration 机制、性能基线方法），M1 在此基线上以增量 migration 新增页面构建后台所需的全部新表。

**Architecture:** 现有 `backend/db/schema.sql` 收编为 `backend/db/migrations/001_baseline.sql`；`backend/db/migrate.js` 维护 `schema_migrations` 表并按序应用迁移；`backend/db/init.js` 改为调用 migrator。老库检测到 `admins` 表已存在且 `schema_migrations` 为空时，将基线标记为已应用而不重跑；新库从头执行。M1 新表一律使用文本 UUID 主键，旧表整数 ID 经 `legacy_id_map` 桥接（D3）。

**Tech Stack:** Node.js 24、Express 4、better-sqlite3 11、node:test（Node 内置测试器）、Next.js 16、React 19、Tailwind CSS 4

## Global Constraints

- 后端为 CommonJS（`require` / `module.exports`）。
- 测试只使用 `node:test` + `node:assert/strict`，不引入 jest。
- 迁移文件一律幂等（`IF NOT EXISTS` / 可重入），命名 `NNN_description.sql` 按文件名排序执行。
- 不修改 `.github/workflows/` 与 `docs/superpowers/specs/` 下的 spec 文档。
- 不执行 `git push`（push 到 main 会触发生产自动部署）。
- 迁移测试使用临时文件库，绝不触碰真实 `backend/db/hkba.db`。
- 部署链路（`db/seed-if-empty.js`、首部署脚本 `db/test-seed-first-deploy.sh`）必须保持可用。

---

## M0：基线准备（本变更实施）

### Task 1: 只读数据盘点脚本

**Files:**
- Create: `backend/scripts/inventory.js`

**Interfaces:**
- 复用 `backend/db/init.js` 的路径解析（`resolveDbPath()`，支持 `HKBA_DB_PATH` 环境变量），另支持 `--db <path>` 参数指向快照文件。
- 以 `readonly + fileMustExist` 打开数据库，绝不写入。
- stdout 输出 JSON：每张表的行数、列清单、脱敏抽样（密码/令牌类列 `[redacted]`，长文本截断）、`schema_migrations` 记录，以及面向验收规范 §5 盘点的 highlights（页面 slug 清单、新闻按发布状态/年份/栏目统计、媒体按类型与体积统计、管理员数量、未读留言、各内容表 active/total）。
- 数据库文件不存在时输出 `{ ok: true, status: "no_database" }` JSON 并正常退出，不崩溃。

- [x] **Step 1: 实现脚本**（见 Files）
- [x] **Step 2: 无库验证**：`node backend/scripts/inventory.js` → 输出 `no_database` JSON，退出码 0。
- [x] **Step 3: 有库验证**：对种子临时库执行 → 14 张表行数齐全、`admins.password` 被脱敏、迁移记录出现。

### Task 2: 版本化 migration 机制

**Files:**
- Create: `backend/db/migrations/001_baseline.sql`（收编原 `schema.sql` 全部建表逻辑，全量 `IF NOT EXISTS`）
- Create: `backend/db/migrate.js`
- Create: `backend/db/migrate.test.js`
- Modify: `backend/db/init.js`
- Modify: `backend/db/test-seed-first-deploy.sh`（复制 `migrate.js` 与 `migrations/`）
- Delete: `backend/db/schema.sql`（已被 001 收编，避免双源真相）

**Interfaces:**
- `migrate(conn, { migrationsDir? })` → `{ applied, baselined, alreadyApplied }`。
- `schema_migrations`：`name TEXT PRIMARY KEY, applied_at TEXT NOT NULL`。
- 基线检测：`schema_migrations` 为空且 `admins` 表存在 → 判定为迁移前老库，记录 `001_baseline.sql` 为已应用而不执行；其余待应用迁移照常执行。
- 每条迁移在事务中执行，失败回滚且不写入记录。
- `init.js` 新增 `resolveDbPath()`（默认 `backend/db/hkba.db`，`HKBA_DB_PATH` 可覆盖），导出保持兼容（`initDatabase/getDb/closeDatabase/DB_PATH`）。

- [x] **Step 1: 编写失败测试**（RED）：新库从头跑、老库基线化、重复运行幂等、基线后迁移照常、失败迁移回滚、`initDatabase` 集成可重入。
- [x] **Step 2: 实现 migrator 与 001 基线**，改造 `init.js`（GREEN）。
- [x] **Step 3: 验证**：`cd backend && npm test` 15/15 通过（含既有 `lib/corsPolicy.test.js` 9 条）。
- [x] **Step 4: 部署链路验证**：`bash backend/db/test-seed-first-deploy.sh` 通过；`seed.js` / `seed-if-empty.js` 在临时库上可用。

### Task 3: 性能基线方法与首轮测量

**方法（写入每个里程碑附录，遵守验收规范 §4：同一机器、同一数据集、相同构建模式）：**

1. 构建耗时：`cd frontend && npm ci && time npm run build`，记录 wall time 与路由清单。
2. 关键路由延迟：生产构建下 `npm start`（前端）与后端运行后，对 `/`、`/news`、`/about`、`/api/health`、`/api/pages`、`/api/news?limit=10` 各执行 5 次 `curl -o /dev/null -s -w '%{time_total}'`，取中位数；对比时使用相同数据集与构建模式，不混用 dev 与 production。
3. 不起常驻 dev server；测量完即停。

- [x] **Step 1: 首轮测量**（结果见文末附录 A）。

---

## M1：数据层新表（本变更实施，2026-07-18 完成）

按 D3/D4，以下迁移全部为**扩展**（只新增表，不改旧表）；旧数据迁移与旧字段收缩分别属于后续里程碑。

**Migration 清单（已实施，编号按 FK 依赖排序）：**

| Migration | 内容 | Spec 依据 |
| --- | --- | --- |
| `002_legacy_id_map.sql` | `legacy_id_map(old_table, old_id INTEGER, new_table, new_id TEXT, source, status CHECK, error, created_at)`，`UNIQUE(old_table, old_id, new_table)`；桥接旧整数 ID 与新 UUID，支持迁移重复运行不重复插入 | 验收 §6 |
| `003_page_nodes.sql` | `page_nodes(id TEXT PK, parent_id 自引用 FK, node_type CHECK(section/page), slug, path UNIQUE, title_zh/en, navigation_status CHECK(visible/hidden/external), external_url, sort_order, published_version_id, draft_version_id, deleted_at, created_at, updated_at)`；slug 同父唯一经表达式索引 `(COALESCE(parent_id,''), slug)`（含顶级节点） | 数据规范 §2.1 |
| `004_page_versions_blocks.sql` | `page_versions(id TEXT PK, page_id FK, revision CHECK>0, status CHECK(draft/published/superseded), seo JSON, created_by/at, published_by/at, source_version_id 自引用, UNIQUE(page_id, revision))`；`page_blocks(id TEXT PK, page_version_id FK, component_type, component_version CHECK>0, sort_order, parent_block_id 自引用 FK, is_visible CHECK(0/1), anchor_id, content_zh/en JSON, settings JSON)`；anchor 部分唯一索引（NULL 不冲突） | 数据规范 §2.2–2.3 |
| `005_media_assets_references.sql` | `media_assets(id TEXT PK, storage_key UNIQUE, original_filename, mime_type, size_bytes CHECK>=0, width, height, checksum, status CHECK(processing/active/failed/trash), alt_zh/en, caption_zh/en, variants JSON, uploaded_by, created_at, deleted_at)`；`media_references(id TEXT PK, media_id FK, ref_type CHECK(page_version/page_block/news_revision/news_block/news_cover/site_setting), ref_id, UNIQUE(media_id, ref_type, ref_id))` | 数据规范 §2.6 |
| `006_news_builder.sql` | `news_items(id TEXT PK, slug UNIQUE, title/summary 中英, cover_media_id FK→media_assets, author_id, published_at, display_year CHECK 四位年份, status CHECK(draft/scheduled/published/withdrawn/trash), current_draft_revision, published_revision, seo JSON)`；`news_revisions(UNIQUE(news_id, revision), snapshot, source_revision_id)`；`news_blocks(news_id FK, revision, block_type, block_version, sort_order, content_zh/en, settings)`，`news.header` 每篇每修订唯一（部分唯一索引）；`news_categories`/`news_tags`（slug UNIQUE）+ `news_category_map`/`news_tag_map`（复合主键关联表，不存自由文本数组） | 数据规范 §2.4–2.5；组件目录 §3.1 |
| `007_rbac_audit_ops.sql` | `roles`、`permissions`、`role_permissions`、`user_roles`（user_id 桥接旧 `admins` 整数 ID）；`audit_events`（actor、action、object_type/id、request_id、before/after 摘要、ip/session，3 个索引）；`preview_tokens`（object_type CHECK(page/news)、token_hash UNIQUE、expires_at，只存哈希）；`redirects`（from_path UNIQUE、status_code CHECK(301/302)）；`publish_records`（action CHECK(publish/withdraw/rollback)、checks_report）；`cleanup_tasks`（status CHECK(pending/running/done/failed)、summary JSON） | 数据规范 §2.8、§10–12；主设计 §5.2、§12 |

**实际表清单（22 张新表）：** `legacy_id_map`、`page_nodes`、`page_versions`、`page_blocks`、`media_assets`、`media_references`、`news_items`、`news_revisions`、`news_blocks`、`news_categories`、`news_tags`、`news_category_map`、`news_tag_map`、`roles`、`permissions`、`role_permissions`、`user_roles`、`audit_events`、`preview_tokens`、`redirects`、`publish_records`、`cleanup_tasks`。

**与 plan 初稿的偏差说明：**

1. **005/006 顺序对调**：`news_items.cover_media_id` 引用 `media_assets(id)`，media 迁移先于 news 执行（plan 已声明编号以实施时为准）。
2. **新增 `news_items` 新表**（plan 初稿未列）：数据规范 §2.4 的 News 模型需要 slug UNIQUE、状态机、`display_year`、双修订指针等旧 `news` 表不具备的字段；按 D3 新建 TEXT UUID 实体，旧表数据日后经 `legacy_id_map` 桥接，扩展期不动旧表。
3. **新增 `role_permissions`**：仅有 roles/permissions/user_roles 无法表达权限归属，按数据规范 §2.8 补齐关联表。
4. **新增运维表 `preview_tokens` / `redirects` / `publish_records` / `cleanup_tasks`**：分别落实数据规范 §11（预览令牌绑定对象+修订+过期）、主设计 §5.2（改路径生成重定向）、数据规范 §10（发布事务写发布记录）与 §12（清理任务可审计输出）。
5. **深度≤3、PageBlock 嵌套≤2 且父必须为布局组件**：SQLite DDL 无法表达递归/跨行规则，落在纯函数库（见下）并在 API 层调用；DDL 仅提供自引用 FK 与 CHECK。
6. **时间戳统一 TEXT ISO**（`datetime('now')` 默认），与 `schema_migrations.applied_at` 一致；旧表 DATETIME 列不动。

**配套纯函数库（backend/lib/，CommonJS）与单测：**

| 文件 | 覆盖 |
| --- | --- |
| `lib/pageTree.js` | `joinPath`（slug 拼接 path）、`wouldCreateCycle`（循环检测）、`depthOf`/`subtreeHeight`、`validateMove`/`validateNewNode`（深度≤3） |
| `lib/newsYear.js` | `displayYearOf`（displayYear 优先，否则 publishedAt 年份）、`isValidDisplayYear`、`latestYearOf`（数据内最大年份，非服务器时钟） |
| `lib/retention.js` | `selectPublishedVersionsToPrune`（保留最近 20 个发布版本）、`selectDraftRevisionsToPrune`（30 天 + 至少保留 20 次）、`selectTrashToPurge`（回收站 30 天） |
| `lib/blockTree.js` | `validateBlockTree`（父必须存在且为 `layout.*`、嵌套≤2 层、循环检测）、`validateNewsBlocks`（`news.header` 恰好一个且在最前） |
| `db/migrations-smoke.test.js` | 每个 migration 冒烟：临时库跑全部迁移，断言 22 表与关键索引存在，并验证同父 slug 唯一、path 唯一、枚举 CHECK、`display_year` 四位年份、`news.header` 部分唯一、复合主键去重、负数体积拒绝等约束行为 |

**M1 验收：**

- [x] `cd backend && npm test` 全绿（49/49：既有 15 条 + lib 21 条 + 冒烟 10 条 + migrate 扩展 3 条口径更新）。
- [x] `node backend/scripts/inventory.js` 对新库列出全部新旧表（36 张：14 旧 + 22 新，含 schema_migrations 另计）。
- [x] 老库（含种子数据）执行 `initDatabase` 后：旧表数据零改动、新表已建立、`legacy_id_map` 为空表就绪（由 migrate 老库基线化测试 + 首部署脚本覆盖）。
- [x] `cd frontend && npm run build` 通过（M1 不动前端，作为回归闸门）。

---

## M2：会话、角色与审计（本变更实施，2026-07-18 完成）

依据主设计 §12、数据规范 §2.8/§13。已批准决策 D3（过渡）：Bearer→Cookie **双模式并行**，前端 UI 全部切完后才移除 Bearer（移除不在本里程碑）。

### 机制设计要点

- **双模式判定顺序**：`middleware/auth.js` 优先校验 HttpOnly 会话 Cookie（`hkba_admin`），无效或缺失时回退 `Authorization: Bearer`；**无效 Cookie 不阻断有效 Bearer 回退**；两者皆无 → 401。`req.authMethod` 记录实际模式。
- **Cookie 属性**：HttpOnly + SameSite=Lax + `Secure`（仅 `NODE_ENV=production`，本地 http 不设否则浏览器拒存）+ `Max-Age=604800`，与现有 7 天 JWT 一致；手写序列化/解析（`lib/cookies.js`），零新增依赖。
- **CSRF 方案**：Cookie 会话的写请求（非 GET/HEAD/OPTIONS）必须携带自定义头 `x-requested-with: XMLHttpRequest`，缺失 → 403。选它的理由：SameSite=Lax 已挡住跨站 POST Cookie 作为基层，自定义头提供跨站表单无法伪造的显式证明，比双提交 token 简单且无状态（不用存/发 token）；Bearer 请求天然免疫 CSRF，豁免检查。
- **登出**：`POST /api/auth/logout` 始终清 Cookie（Max-Age=0），能识别身份时写审计；过期会话也能正常登出。
- **RBAC**：`lib/rbac.js` 种子 3 角色（super_admin/editor/publisher）× 6 权限点（content.read/content.write/publish/rollback/media.delete/system.admin），全部 `INSERT OR IGNORE` 幂等，随 `initDatabase()` 执行；旧 `admins` 整数 ID 经 `user_roles` 桥接；**首个 admin 自动挂 super_admin**（防自锁）。`middleware/requirePermission(code)` 查询失败闭门（403）。挂载点仅两处验证机制：改密（`content.read`）、上传（`content.write`）；全量铺开随各模块 API。
- **审计**：`lib/audit.js` 写 `audit_events`（actor、action、object、detail JSON、ip、session、user_agent）；接入登录成功/失败、登出、改密；**写库失败只 console.error，绝不让主流程 500**（测试覆盖 DROP TABLE 后登录仍 200）。
- **前端**：`adminApi.ts` 一律 `credentials:'include'` + CSRF 头，localStorage Bearer 作回退（过渡期）；layout 改 `/api/auth/verify` 探测会话（替代 localStorage 检查）；登录页支持 `?next=` 回跳（仅接受 `/admin` 内部路径防开放重定向）；401 时先派发 `hkba:session-expired` 事件再跳登录——`lib/sessionGuard.ts` 让表单页快照/恢复未保存编辑（30 分钟有效），已在新闻页接线作范式。

### 偏差说明

1. CSRF 采用自定义头方案而非双提交 token（理由见上）。
2. `audit_events.user_agent` 经 `008_audit_events_user_agent.sql`（ALTER ADD COLUMN）补充；SQLite 无 `ADD COLUMN IF NOT EXISTS`，单次应用由 `schema_migrations` 保证，区别于纯幂等迁移文件。
3. `requirePermission` 只挂改密与上传两个端点验证机制，其余端点随各模块 API 里程碑铺开。
4. 前端保留 localStorage Bearer 回退；`Set-Cookie` 用手写序列化而非 `res.cookie`（零依赖且可纯函数测试）。
5. 会话过期表单保留机制先在新闻页接线，其余表单随页面构建器 UI 里程碑推广。
6. 部署自测脚本 `test-seed-first-deploy.sh` 增补复制 `lib/rbac.js`（init.js 新依赖；生产 rsync 全仓不受影响）。

### M2 验收

- [x] `cd backend && npm test` 全绿（71/71：既有 49 + cookies 4 + rbac 2 + audit 3 + HTTP 集成 13）。
- [x] 双模式鉴权 / Cookie 优先 / Bearer 回退 / 缺失 401 / logout 清 Cookie / CSRF 拦截与豁免 / 权限放行与拒绝 / 审计写入与失败容忍，全部经临时库 + 原生 HTTP 实测（`backend/test/authSession.test.js`，未引入 supertest）。
- [x] `JWT_SECRET` 缺失拒绝启动行为不变（middleware/auth.js 顶部检查保留）。
- [x] `cd frontend && npm run build` 通过；`test-seed-first-deploy.sh` 通过。

---

## M3：组件注册表 + 页面树/草稿/区块 API（本变更实施，2026-07-18 完成）

依据主设计 §5/§8/§10、目录规范 §1-§8、数据规范 §2.7。交付两部分：后端组件注册表（契约唯一事实源）与页面树/草稿/区块管理 API。

### 组件注册表（`backend/components/registry/`）

- **30 个组件定义**（catalog §1-§8）：newsDisplay 6（news.grid/list/featured/archive/category-tabs/related）、newsBlocks 8（news.header/media.image/media.gallery/media.video/content.rich-text/content.quote/content.embed/association.related-pages）、content 5、association 6、layout 5（含 section/columns/grid 等布局容器）。
- **手写 schema 校验器**（`components/validate.js`）而非 zod：零新增依赖（对齐 Global Constraints），字段类型/必填/枚举/范围/长度/数组项 + 未知字段拒绝（data-api §5），纯函数可单测。
- **注册表 API**：`getDefinition` 扫数组（支持运行时注入测试定义）、`validateBlockConfig`（错误带 `contentZh./contentEn./settings.` 前缀）、`migrateConfig` 链式纯函数、`listDefinitions` 去函数化输出元数据、`allowsPageType` 页面类型约束。
- 共享 `yearMode` 查询设置 + `specificYearRule`（yearMode=specific 时 year 必填）用于全部 6 个新闻展示组件。

### 页面/草稿/区块 API（`routes/admin/pages.js` + `routes/admin/components.js`）

统一响应格式 `{success, data|error{code,message,fields}, meta.requestId}`（`lib/respond.js` 的 requestContext 中间件生成 requestId；错误码 VALIDATION_FAILED/NOT_FOUND/FORBIDDEN/REVISION_CONFLICT 409/REFERENCE_EXISTS 409/INTERNAL_ERROR）。全部写端点挂双模式鉴权 + `requirePermission` + 审计。

- `GET /api/admin/components/definitions`：注册表元数据（content.read）。
- 页面树：`GET /api/admin/pages/tree`（嵌套 + 状态标记）、`POST /api/admin/pages`、`PATCH /api/admin/pages/:id`（标题/导航状态/改 slug）、`POST /api/admin/pages/:id/move`（重算子树 path，禁止移入自身后代）、`POST /api/admin/pages/:id/duplicate`（连同草稿区块复制）、`DELETE /api/admin/pages/:id`（回收站，`childrenStrategy=refuse|trash|move`）、`POST /api/admin/pages/:id/restore`。
- 草稿：`GET /api/admin/pages/:id/draft`（首次访问从 published 复制或建空草稿，`lib/drafts.js` getOrCreateDraft）、`PATCH .../draft`（标题等元数据）。
- 区块：`POST/PATCH/DELETE /api/admin/pages/:id/draft/blocks[/:blockId]`、`POST .../blocks/reorder`（全排列校验）、`POST .../blocks/:blockId/duplicate`。全部经 `applyDraftMutation`：`expectedRevision` 乐观锁（不符 → 409 REVISION_CONFLICT 带 currentRevision）+ `mutationId` 幂等重放（`009_mutation_log.sql`，命中直接返回已存响应）。
- 树规则：最多 3 级、slug 同级唯一跨级可复用、布局嵌套仅 2 层且仅 layout 可作父、删除有子区块的块 → 409 REFERENCE_EXISTS。

### 偏差说明

1. 实际路径带 `/api` 前缀（`/api/admin/pages/tree` ↔ spec `/admin/pages/tree`），与既有路由一致。
2. **草稿校验宽容**：区块保存时 `contentEn` 的 required 错误被过滤（`validateBlockConfig` 的 `allowMissingEn` 选项）——spec §10 允许中文先存草稿，英文完整性是发布检查项（M6）而非保存检查项。
3. `news.related` 的 `allowedPageTypes` 为 `['news']`（仅新闻详情模板），newsDisplay helper 因此支持参数覆盖；媒体类区块（media.image 等）按目录规范页面/新闻共享 `['news','page']`。
4. slug 改名：未发布节点直接改；已发布节点写 `redirects` 表（upsert from_path → 301）。预览/发布端点留 M6。
5. duplicate 区块时 anchor 置 NULL，避免撞 `(version_id, anchor)` 唯一索引。
6. `DELETE` 的 `expectedRevision` 经 query 传递（body 亦可），兼容不发 body 的客户端。
7. publisher 角色 403 走 `requirePermission` 既有响应形状；新统一错误格式仅用于 M3 新端点。

### M3 验收

- [x] `cd backend && npm test` 全绿（107/107：M2 的 71 + validate 6 + registry 9 + adminPages 集成 21）。
- [x] 集成测试起真实 HTTP + 临时库（`test/adminPages.test.js`，21 条：页面树 CRUD/移动/回收站策略/恢复、草稿乐观锁与幂等重放、区块嵌套/重排/复制/删除约束、注册表校验拒绝、权限拒绝）。
- [x] `cd frontend && npm run build` 通过（M3 不碰前端）；`test-seed-first-deploy.sh` 通过（009 随 migrate 应用）。

---

## M4：媒体上传管线与媒体库 API（本变更实施，2026-07-18 完成）

依据数据规范 §2.6/§7、决策 D8（稳定 ID 引用）与 D10（首期本地存储）。交付：媒体资产存储校验管线、引用提取同步、媒体库管理 API。

### 实施要点

- **存储管线**（`lib/mediaStore.js`）：魔数嗅探判定类型（不信客户端 MIME）；允许 JPEG/PNG/WebP/AVIF/SVG/PDF，图片 ≤15MB、PDF ≤30MB；SVG 存储前清理（script/foreignObject 元素、on* 事件属性、javascript: URL）；sha256 校验和；`storage_key = media/<uuid>.<ext>` 写入 uploads 根（`HKBA_UPLOADS_DIR` 可覆盖，测试用临时目录）。
- **尺寸提取**（`lib/imageSize.js`）：手写 PNG/JPEG/GIF/WebP/AVIF 头部解析，只读头不解码；解析失败返回 null 而非拒绝上传（元数据尽力而为）。
- **引用同步**（`lib/mediaReferences.js`）：注册表 schema 的媒体字段以 `media: true` 标记（`shared.js` 的 `mediaRefField`）；递归遍历 contentZh/contentEn/settings（含数组项与嵌套对象）提取媒体 ID；区块 创建/更新/复制/删除 及页面 duplicate 时在事务内同步 `media_references`；悬空 ID 不记录（保 FK 完整）。
- **API**（`routes/admin/media.js`，统一响应格式 + 双模式鉴权 + 审计）：`POST /api/admin/media/uploads`（content.write，201 返回 asset JSON）、`GET /api/admin/media`（q/kind/status/unused 筛选 + 分页，默认排除回收站）、`PATCH /api/admin/media/:id`（中英 alt/caption、檔名）、`GET /api/admin/media/:id/references`、`DELETE /api/admin/media/:id`（入回收站，保留磁盘文件）、`DELETE /api/admin/media/:id/permanent`（media.delete 权限；有引用 → 409 REFERENCE_EXISTS；删行删文件）。

### 偏差说明

1. D10 本地存储 → 单段 multipart `POST /uploads` 合并 spec §7 的签名直传三步（uploads → 对象存储 → complete）；`variants` 保持 `[]`，响应式变体生成留给后续图像处理里程碑（引入 sharp 等原生依赖需单独评估，零依赖约束优先）。
2. 永久删除严格化：任何已记录引用（含草稿区块引用）都阻止，spec 原文仅禁"正式版本引用"——草稿引用被删同样破坏内容。
3. GIF/ICO 不在 spec §7 允许列表，新管线不支持；旧 `/api/upload`、`/api/media` 路由保持不动兼容旧后台（渐进升级）。
4. 无新 migration：media_assets/media_references 表由 005 提供；无新 npm 依赖（multer 既有）。

### M4 验收

- [x] `cd backend && npm test` 全绿（124/124：M3 的 107 + mediaStore 单元 6 + adminMedia 集成 11）。
- [x] 集成测试覆盖：上传元数据与审计、缺文件/非法类型/超限拒绝（UPLOAD_REJECTED 信封）、SVG 清理落地、q/kind/unused 筛选、alt 更新校验、引用随区块增删改镜像、永久删除的引用保护与 media.delete 权限、回收站行为。
- [x] `test-seed-first-deploy.sh` 通过；`cd frontend && npm run build` 通过（M4 不碰前端）。

---

## 验证方式（M0 本变更）

| 检查 | 命令 | 期望 |
| --- | --- | --- |
| 后端单元测试 | `cd backend && npm test` | 71/71 通过（M0 交付时 15/15，M1 后 49/49） |
| 首部署种子链路 | `bash backend/db/test-seed-first-deploy.sh` | 通过 |
| 盘点脚本（无库） | `node backend/scripts/inventory.js` | 输出 `no_database` JSON，退出码 0 |
| 盘点脚本（有库） | `node backend/scripts/inventory.js --db <快照>` | 全表行数 + 脱敏抽样 JSON |
| 前端构建 | `cd frontend && npm run build` | 通过，耗时见附录 A |

## 回滚策略

- **代码**：本变更在 `phase2/m0-m1-data-layer` 分支，未 push；回滚即还原分支提交，不触发生产。
- **数据库**：`001_baseline.sql` 为纯扩展且幂等；老库只新增 `schema_migrations` 表与一行记录，不改动任何既有表与数据。回滚代码后遗留的 `schema_migrations` 表无害（"扩展"阶段产物），后续迁移可继续在其上前进。
- **服务器执行迁移前**：先 `cp backend/db/hkba.db backend/db/hkba.db.bak-<date>` 快照，再用 `node backend/scripts/inventory.js --db <快照>` 核对盘点。
- **内容回滚与代码回滚分离**：内容版本回退属于后续发布服务能力，不依赖本次机制回滚（验收 §10）。

## 风险与缓解

| 风险 | 缓解 |
| --- | --- |
| 本地 Node 24 下 `better-sqlite3@11` 无预编译二进制，node-gyp 若取到含空格路径的 Python 会编译失败 | 环境级修复：以无空格路径 Python 安装（`npm_config_python=/usr/bin/python3 npm install`）；不升级依赖、不改 lock，生产 `npm ci` 行为不变 |
| 生产老库首次运行新 `init.js` 触发基线检测误判 | 检测条件保守（`schema_migrations` 为空 **且** `admins` 存在）；基线 SQL 全量 `IF NOT EXISTS`，即使执行也不改数据；上线前先在生产快照上演练 |
| `schema.sql` 删除后有残留引用 | 已全仓 grep：仅 `init.js` 与首部署脚本引用，均已更新 |
| 盘点脚本对在线 WAL 库只读打开 | better-sqlite3 `readonly + fileMustExist` 不加写锁；脚本无写路径 |
| 迁移文件被改名导致历史记录错乱 | 迁移名一旦合入即冻结；新变更只能追加更高编号文件（计划约定） |

## 附录 A：首轮性能与环境基线（2026-07-18，本机 macOS arm64，Node v24.15.0）

- `cd frontend && npm install && npm run build`：**通过**，wall time **13.2s**；Next.js 16.2.10（webpack 构建），产出 20 条路由（静态：`/`、`/about`、`/news`、`/events`、`/members`、`/team`、`/contact`、后台各页；动态：`/news/[id]`；Proxy 中间件生效）。
- `cd backend && npm install && npm test`：**15/15 通过**（migrate 6 条 + corsPolicy 9 条），耗时约 0.5s。
- `bash backend/db/test-seed-first-deploy.sh`：通过。
- 盘点脚本：无库时优雅输出；对种子临时库输出 14 张表（banners 3、news 6、partners 11、team_members 4、milestones 4、stats 4、pages 2、contact_info 9、admins 1 等），密码列已脱敏。
- 关键路由延迟基线：需在本地同时起动前后端测量，留待 M1 变更前后按 §Task 3 方法补测对比。

## 附录 B：M1 数据层实施结果（2026-07-18）

- `cd backend && npm test`：**49/49 通过**（corsPolicy 9、migrate 机制 6、migration 冒烟 10、pageTree 7、newsYear 4、retention 4、blockTree 5、`initDatabase` 集成另计于 migrate 文件内），耗时约 0.3s。
- `bash backend/db/test-seed-first-deploy.sh`：通过（7 个迁移文件随部署复制）。
- 盘点脚本对种子临时库：36 张表（14 旧 + 22 新）、7 条迁移记录，密码列脱敏正常。
- `cd frontend && npm run build`：**通过**（M1 未触碰前端，作回归确认）。
- 全部迁移幂等：临时库重复执行 `migrate` 第二次 `applied=[]`；老库基线化后 002–007 照常执行、旧表数据零改动（测试覆盖）。

## 附录 C：M2 会话、角色与审计实施结果（2026-07-18）

- `cd backend && npm test`：**71/71 通过**，新增 22 条（cookies 4、rbac 2、audit 3、authSession HTTP 集成 13），总耗时约 2s。
- `cd frontend && npm run build`：**通过**（登录页 `useSearchParams` 已包 Suspense）。
- `bash backend/db/test-seed-first-deploy.sh`：通过（增补 `lib/rbac.js` 复制后）。
- 依赖变化：**零新增依赖**（Cookie 序列化手写、测试用 Node 原生 fetch/http，未引入 supertest/cookie-parser）。
