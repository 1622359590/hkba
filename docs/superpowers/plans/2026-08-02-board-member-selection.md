# Board Member Selection Implementation Plan

1. Extract deterministic board-member selection into a tested helper.
2. Add `selectedMemberIds` to the backend component definition.
3. Add the board-member picker and ordering controls to the Studio property form.
4. Pass structured association people into the property form.
5. Use the helper in the public and Studio board renderer.
6. Run focused tests, production build, and browser verification.

